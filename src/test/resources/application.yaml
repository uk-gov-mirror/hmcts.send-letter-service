spring:
  application:
    name: Send Letter Provider
  jackson:
    serialization:
      write_dates_as_timestamps: false

file-upoad:
  retries: ${DEFAULT_RETRIES:5}
  wait-time-in-milli: ${DEFAULT_WAIT_TIME_IN_MILLI:2000}